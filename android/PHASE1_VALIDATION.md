# FASE 1.1 — validação real

Não considerar nenhum item concluído sem execução real.

## Gradle
`gradlew.bat clean`
`gradlew.bat assembleDebug --stacktrace`

## ExtractorEngine
Após instalar o APK, confirmar no Logcat a inicialização real do runtime.

## FFmpeg
Ainda não integrado nesta cópia. A solução para conversão MP3 será validada separadamente antes de entrar no pipeline principal.
