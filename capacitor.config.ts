import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.downloaderandroid',
  appName: 'MusicasAndroid',
  webDir: 'frontend'
};

export default config;
